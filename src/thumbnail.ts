const createThumbnailFromImage = async (
    imagePath: string,
    thumbnailConfig: object,
  ): Promise<String> => {

    return "";
  }

  const createThumbnailFromVideoSources = async (
    imagePath: string,
    thumbnailConfig: object,
  ): Promise<String> => {

    return "";
  }  